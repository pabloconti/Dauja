{
  "name": "dauja-web",
  "version": "1.0.0",
  "private": true,
  "description": "Sitio estático de Dauja S.R.L. en HTML, CSS y JavaScript puro",
  "scripts": {
    "dev": "node server.cjs",
    "build": "node build.cjs",
    "check": "node --check script.js && node --test asset-versions.test.cjs && node validate.cjs",
    "preview": "node server.cjs --dist",
    "check:build": "node validate.cjs --dist && node --check dist/script.js",
    "export:hosting": "npm run build && npm run check && npm run check:build && node export-hosting.cjs"
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}
